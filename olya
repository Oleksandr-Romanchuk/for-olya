<!DOCTYPE html>
<html lang="uk">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<link rel="icon" href="heart.jpeg">
<title>Секретний файл Олі</title>

<style>
body {
    margin: 0;
    background: #111;
    color: white;
    text-align: center;
    font-family: Arial, sans-serif;
    overflow: hidden;
}

h1 {
    margin-top: 80px;
}

/* Кнопка */
button {
    padding: 15px 30px;
    font-size: 18px;
    background-color: hotpink;
    border: none;
    border-radius: 10px;
    cursor: pointer;
    position: relative;
    transition: all 0.3s ease;
}

/* Ефект втечі */
button:hover {
    transform: translate(
        calc(20px - 40px * random()),
        calc(20px - 40px * random())
    );
}

/* Плавна поява блоку */
#hiddenMessage {
    opacity: 0;
    transition: opacity 1.5s ease;
    margin-top: 30px;
}

#hiddenMessage.show {
    opacity: 1;
}

img {
    width: 80%;
    max-width: 300px;
    border-radius: 20px;
    margin-top: 20px;
    box-shadow: 0 0 20px hotpink;
}

/* Сердечка */
.heart {
    position: fixed;
    top: -10px;
    color: pink;
    font-size: 20px;
    animation: fall linear forwards;
}

@media (max-width: 600px) {
    h1 {
        font-size: 26px;
    }

    h2 {
        font-size: 18px;
        padding: 0 10px;
    }

    button {
        font-size: 16px;
    }
}
@keyframes fall {
    to {
        transform: translateY(100vh);
    }
}
</style>
</head>

<body>

<h1>⚠️ УВАГА ⚠️</h1>
<h2>Ви відкрили секретний файл Олі Кравченко 😏</h2>

<button id="loveButton">Натисни, будь ласка</button>

<div id="hiddenMessage">
    Вітаю зі здачею 😌 <br><br>
    Ich liebe dich ❤️ <br><br>
    <img src="olya.jpeg" alt="Оля">
</div>

<script>

let heartsInterval;

const button = document.getElementById("loveButton");
const message = document.getElementById("hiddenMessage");

/* Втеча кнопки */
button.addEventListener("mouseover", function() {
    const x = Math.random() * 200 - 100;
    const y = Math.random() * 200 - 100;
    button.style.transform = `translate(${x}px, ${y}px)`;
});

/* Натискання */
button.addEventListener("click", function() {
    message.classList.add("show");
    startHearts();
});

/* Сердечка */
function createHeart() {
    const heart = document.createElement("div");
    heart.classList.add("heart");
    heart.innerHTML = "❤️";
    heart.style.left = Math.random() * 100 + "vw";
    heart.style.animationDuration = (Math.random() * 3 + 2) + "s";
    document.body.appendChild(heart);

    setTimeout(() => {
        heart.remove();
    }, 5000);
}

function startHearts() {
    heartsInterval = setInterval(createHeart, 300);
}

</script>

</body>
</html>
